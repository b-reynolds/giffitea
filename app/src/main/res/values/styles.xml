<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/draculaCurrentLine</item>
        <item name="colorPrimaryDark">@color/draculaCurrentLine</item>
        <item name="colorAccent">@color/draculaRed</item>
    </style>

</resources>
